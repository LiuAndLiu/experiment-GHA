name: Dispatch test
on: workflow_dispatch

jobs:
  workflow-dispatch-test:
    runs-on: ubuntu-latest
    env: 
      INFO: ${{ toJSON(github.event) }}
    steps:
      - name: Event info
        run: echo $INFO